{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm --prefix react ci",
  "buildCommand": "npm --prefix react run build",
  "outputDirectory": "react/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
