{
  "name": "guiguzi-deploy",
  "version": "0.3.7",
  "private": true,
  "scripts": {
    "build": "npm --prefix react run build",
    "test": "npm --prefix react test -- --watchAll=false"
  },
  "engines": {
    "node": "20.x"
  }
}
